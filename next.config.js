/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
      // protocol: 'https',
      domains: ['images.unsplash.com', 'plus.unsplash.com'],
  }
     
}

module.exports = nextConfig